# Zarf configuration for local development
# Usage: zarf package create dist/ --confirm

[package.create.set]
# Package version
VERSION = "0.1.0"

# Operator image configuration
OPERATOR_IMAGE = "ghcr.io/enel1221/zarf-operator"
OPERATOR_IMAGE_TAG = "main"

# Sidecar image configuration
SIDECAR_IMAGE = "ghcr.io/enel1221/zarf-operator-sidecar"
SIDECAR_IMAGE_TAG = "main"
