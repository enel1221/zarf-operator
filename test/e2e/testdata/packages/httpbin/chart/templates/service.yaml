apiVersion: v1
kind: Service
metadata:
  name: httpbin
  namespace: {{ .Release.Namespace }}
  labels:
    app: httpbin
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: httpbin
